https://codepen.io/stevenpmcfarlane/pen/eYdNqgV?editors=1100   

 1. What is Semantic HTML? 	Semantic HTML is a mark-up language that virtually all websites use to display elements on a public facing wepage.
    2. What is HTML used for? 	HTML is used to display what the developer wants on the webpage in an organized, readable, and attractive fashion.
    3. What is an attribute and where do we put it? 	An attribute is information related to a tag or element that is not put on display on the website.
    4. What is the h1 tag used for? How many times should I use it on a page?	The h1 tag is for the most important header and should probably be used once on a page.
    5. Name two tags that have required attributes	<a> will always have an href attribute and <img> will have a src attribute.
    6. What do we put in the head of our HTML document?		Information about the file you are working on, for instance which version of HTML. It is not publicly displayed.
    7. What is an id? 	An id marks a unique element in html and should not be used to pick out more than that specific element
    8. What elements can I add an id to? 	The ones for which you provide a name
    9. How many times can I use the same id on a page? 	Only once per element
    10. What is a class? 	A class can group several elements of the same type that you might want to apply the same styling to.
    11. What elements can I add a class to? 	The onse for which you provide a name
    12. How many times can I use the same class on a page? 	You can use class multiple times
    13. How do I get my link to open in a new tab?	Add the target="_blank" attribute in the tag
    14. What is the alt attribute in the image tag used for? 	If the image won't load, it will open a new window
    15. How do I reference an id? use '#' and the id
    16. What is the difference between a section and a div? 	Section is a semantic tag and div isn't
    17. What is CSS used for? 	CSS styles the page to make it more attractive and easier to use. It adds design to the HTML code.
    18. How to we select an element? Example - every h2 on the page?   Write the name of it and then braces, ex. h2{}
    19. What is the difference between a class and an id? - Give me an example of when I might use each one. 	You can use a class for more than one element, for example if you want to make all of your h2's the same color, no matter where they are on the page, you can use .h2 and apply the color you want. For an id, you use #id and you'd only apply it to one element, so if you want to draw attention to one particular heading, you give it its own unique id and apply a different color to that single heading
    20. How do we select classes in CSS? 	Use '.' and then the name of the class.
    21. How do we select a p element with a single class of “human””? 
    22. What is a parent child selector? When would this be useful? 	A parent child selector will apply styling to all the "children" of the parent. So for instance if you're making an ordered list, you might want them all to share some style property - the name of  the list and each item on the list
    23. How do you select all links within a div with the class of sidebar?  	Not sure about this one; I have to ask
    24. What is a pseudo selector?	A pseudo selector is when you provide extra information for a selector that might be going through different states (ex. 'hover' or 'onclick')
    25. What do we use the change the spacing between lines? 	Use the line-height property
    26. What do we use to change the spacing between letters?	Use the letter-spacing property
    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize? 	Respectively, use text-transform: uppercase; text-transform: lowercase; and text-transform; 
    28. How do I add a 1px border around my div that is dotted and black?     div{border: 1px dotted black}
    29. How do I select everything on the page?		Use the universal selector '*'
    30. How do I write a comment in CSS?	/*Write a comment using backslash and asterisks*/
    31. How do I find out what file I am in, when I am using the command line?   pwd
    32. Using the command line - how do I see a list of files/folders in my current folder?	ls
    33. How do I remove a file via the command line? Why do I have to be careful with this? 	rm You have to be careful because it won't ask twice or send the file to the recycle bin. It is just gone and that's it - there's no second chance.
    34. Why should I use version control? 	It allows you to work on your part of the project without intefering with the larger remote project. The larger remote project stays in tact until your project is merged in.
    35. How often should I commit to github? 	You should commit after making changes to the repo, right before pushing it back to GitHub.	
    36. What is the command we would use to push our repo up to github? git push -u origin 'firstName-lastName'
    37. Walk me through Lambda's git flow. 1. Fork, 2. clone, 3. branch, 4. work on your project on your local machine, 5. Add (fill package in post office), 6. Commit (address and seal package), 7. Push to remote repo (drop in the mailbox)

Stretch Questions

    1. What is the difference between an inline element and a block element? 	An inline element won't break between lines - it will just apply to the line it's in. A block element applies to the whole group and then will break off before the next thing. So for instance <p> is a block element but <a> is inline
    2. What happens when an element is positioned absolutely? Not sure
    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width? 
    4. Name 3 elements that are diplay block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default
    5. In your own words, explain the box model. What is the "fix" for the box model, in other words, how do we make all elements respect the width we've given them? 
